"""
Minimal shim for osi (GEE_notebook_Forestry). All configuration is in pyproject.toml (PEP 517/518).
Kept for tools that still expect setup.py at repo root.
"""
from setuptools import setup

setup()
